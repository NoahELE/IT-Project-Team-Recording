import {Typography} from "@mui/material";

export default function HomeView() {
    return (
        <div>
            <Typography variant="h1">Recording</Typography>
            <Typography variant="body1">C-LARA currently only uses TTS audio Human audio is better for high-quality
                texts. Many examples on LARA site Task: implement human audio recording tool</Typography>
        </div>
    );
}
